import { TouchableOpacity } from "react-native";
import styled from "styled-components";

const AuthAvatarBtn = styled(TouchableOpacity)`
    position: absolute;
    display: flex;
    justify-content: center;
    align-items: center;
    top: -60px;
    left: 128px;
`;

export default AuthAvatarBtn;
