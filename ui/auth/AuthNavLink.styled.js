import { TouchableOpacity } from "react-native";
import styled from "styled-components";

const AuthNavLink = styled(TouchableOpacity)`
  background-color: transparent;
`;

export default AuthNavLink;
