import { ImageBackground } from "react-native";
import styled from "styled-components";

const CreatePostsImgIcon = styled(ImageBackground)`
  width: 60px;
  height: 60px;
`;

export default CreatePostsImgIcon;
