import { TouchableOpacity } from "react-native";
import styled from "styled-components";

const CreatePostsImgPlaceholderContainer = styled(TouchableOpacity)`
  position: relative;
  width: 100%;
  margin-bottom: 32px;
`;

export default CreatePostsImgPlaceholderContainer;
