import MapView from "react-native-maps";
import styled from "styled-components";

const MapScreenView = styled(MapView)`
  background-color: #fff;
  height: 100%;
  width: 100%;
`;

export default MapScreenView;
