import { View } from "react-native";
import styled from "styled-components";

const CreatePostsLocationIconContainer = styled(View)`
  display: flex;
  justify-content: center;
  align-items: center;
  width: 24px;
  height: 24px;
  margin-right: 4px;
`;

export default CreatePostsLocationIconContainer;
