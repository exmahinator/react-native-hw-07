import { ImageBackground } from "react-native";
import styled from "styled-components";

const CreatePostsImg = styled(ImageBackground)`
  width: 100%;
  height: 100%;
`;

export default CreatePostsImg;
