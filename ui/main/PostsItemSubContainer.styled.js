import { View } from "react-native";
import styled from "styled-components";

const PostsItemSubContainer = styled(View)`
  display: flex;
  justify-content: space-between;
  flex-direction: row;
`;

export default PostsItemSubContainer;
