import { ImageBackground } from "react-native";
import styled from "styled-components";

const CreatePostsImgPlaceholderIcon = styled(ImageBackground)`
  position: absolute;
  left: 50%;
  top: 50%;
  width: 60px;
  height: 60px;
  transform: translate(-30px, -30px);
`;

export default CreatePostsImgPlaceholderIcon;
