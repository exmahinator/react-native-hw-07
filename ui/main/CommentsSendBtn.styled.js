import { TouchableOpacity } from "react-native";
import styled from "styled-components";

const CommentsSendBtn = styled(TouchableOpacity)`
  display: flex;
  justify-content: center;
  align-items: center;
  height: 34px;
  width: 34px;
  background-color: #ff6c00;
  /* border-radius: 50%; */
  border-radius: 17px;
`;

export default CommentsSendBtn;
