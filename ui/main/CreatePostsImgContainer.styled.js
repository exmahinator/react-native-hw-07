import { View } from "react-native";
import styled from "styled-components";

const CreatePostsImgContainer = styled(View)`
  position: relative;
  margin-bottom: 32px;
`;

export default CreatePostsImgContainer;
