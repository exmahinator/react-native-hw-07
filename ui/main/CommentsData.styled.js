import { Text } from "react-native";
import styled from "styled-components";

const CommentsData = styled(Text)`
  font-family: "Roboto-Medium";
  font-size: 10px;
  line-height: 12px;
  color: #bdbdbd;
  text-align: right;
`;

export default CommentsData;
