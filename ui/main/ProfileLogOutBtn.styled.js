import { TouchableOpacity } from "react-native";
import styled from "styled-components";

const ProfileLogOutBtn = styled(TouchableOpacity)`
  position: absolute;
  display: flex;
  align-items: center;
  justify-content: center;
  top: 22px;
  right: 16px;
  width: 24px;
  height: 24px;
`;

export default ProfileLogOutBtn;
