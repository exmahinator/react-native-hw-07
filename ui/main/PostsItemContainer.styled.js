import { View } from "react-native";
import styled from "styled-components";

const PostsItemContainer = styled(View)`
  display: flex;
  margin-bottom: 32px;
  /* width: 100%; */
`;

export default PostsItemContainer;
