import { View } from "react-native";
import styled from "styled-components";

const PostsOwnerContainer = styled(View)`
  display: flex;
  align-items: center;
  align-self: flex-start;
  margin-bottom: 32px;
  flex-direction: row;
`;

export default PostsOwnerContainer;
