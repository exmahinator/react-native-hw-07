import { Text } from "react-native";
import styled from "styled-components";

const CreatePostsInfo = styled(Text)`
  font-family: "Roboto-Medium";
  font-size: 16px;
  line-height: 19px;
  color: #bdbdbd;
`;

export default CreatePostsInfo;
