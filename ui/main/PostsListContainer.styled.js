import { ScrollView } from "react-native";
import styled from "styled-components";

const PostsListContainer = styled(ScrollView)`
  display: flex;
  width: 100%;
`;

export default PostsListContainer;
