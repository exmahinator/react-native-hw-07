import { TouchableOpacity } from "react-native";
import styled from "styled-components";

const CreatePostsSnapBtn = styled(TouchableOpacity)`
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-30px, -40px);
`;

export default CreatePostsSnapBtn;
