import { View } from "react-native";
import styled from "styled-components";

const CommentsSubContainer = styled(View)`
    display: flex;
    flex-direction: row;
    align-items: flex-start;
    margin-bottom: 24px;
    width: 320px;
`;

export default CommentsSubContainer;
